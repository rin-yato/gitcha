pre-commit:
  commands:
    lint-staged:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
      run: "bun run fix && git update-index --again"

pre-push:
  commands:
    ci-check:
      run: "bun run ci"

